import 'package:flutter/material.dart';

class JobShift extends StatelessWidget {
  const JobShift({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
